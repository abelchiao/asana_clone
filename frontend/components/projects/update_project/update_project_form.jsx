import React from 'react';
import { withRouter } from 'react-router-dom';

class UpdateProjectForm extends React.Component {
  constructor(props) {
    super(props)
  };

  render() {
    return (
      <div>Update Page Form</div>
    );
  };
};

export default withRouter(UpdateProjectForm);